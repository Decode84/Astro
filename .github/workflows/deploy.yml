name: Deploy
on:
  push:
    branches: [dev]
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy nodejs
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.USER_USERNAME }}
          port: ${{ secrets.PORT }}
          passphrase: ${{ secrets.USER_PASS }}
          
          script: |
            mkdir test
            cd test
            git clone git@github.com:KB885/Astro.git
            echo "Deployment was successful"
